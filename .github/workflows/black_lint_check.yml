name: Lint

on:
  pull_request:
    paths:
     - "test_code/**.py"

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: psf/black@stable
        with:
          options: "--check --verbose"
          src: "./test_code/"
          version: "21.5b1"